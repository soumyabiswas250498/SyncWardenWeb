# REST API base URL (backend mounts routes under /api/v1).
# Check your local backend/.env PORT -- the committed backend/.env.example
# defaults to 5000, but your local backend may run on a different port.
VITE_API_BASE_URL=http://localhost:5000/api/v1
